# Add-on dependencies — plain uvicorn (no [standard] extras) so the image
# builds on armv7 where uvloop is unavailable.
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
click>=8.1.0
rich>=13.0.0
pydantic>=2.5.0
